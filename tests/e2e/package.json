{
    "name": "setono-sylius-completeness-e2e",
    "description": "Playwright end-to-end specs for the completeness plugin admin UI",
    "private": true,
    "license": "MIT",
    "scripts": {
        "test": "playwright test",
        "test:ui": "playwright test --ui",
        "report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "^1.48.0"
    }
}
